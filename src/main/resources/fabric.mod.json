{
	"schemaVersion": 1,
	"id": "${modId}",
	"version": "${version}",
	"name": "${modName}",
	"description": "${modDescription}",
	"authors": [
		"${authors}"
	],
	"contact": {
		"homepage": "${homepage}",
		"issues": "${issues}",
		"sources": "${sources}"
	},
	"license": "${license}",
	"icon": "assets/${modId}/icon.png",
	"environment": "*",
	"entrypoints": {
		"main": [
			"net.syrupstudios.plushiefriends.loaders.fabric.FabricEntrypoint"
		],
		"client": [
			"net.syrupstudios.plushiefriends.loaders.fabric.FabricEntrypoint"
		]
	},
	"mixins": [
		"${modId}.mixins.json"
	],
	"depends": {
		"fabricloader": ">=${fl}",
		"minecraft": "${mc}",
		"fabric-api": ">=${fapi}",
		"java": ">=${java}"
	}
}
